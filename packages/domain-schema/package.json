{
  "name": "@cyntientops/domain-schema",
  "version": "1.0.0",
  "description": "Zod schemas and TypeScript types for CyntientOps data models",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "generate-json-schemas": "node scripts/generate-json-schemas.js",
    "test": "jest"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.21.4"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}